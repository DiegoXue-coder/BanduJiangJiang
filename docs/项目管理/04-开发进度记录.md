# 开发进度记录（跨设备协作日志）

> 这份文件是两台设备上 Claude 实例之间的"桥"——不直接对话，通过这份文件 + git 同步互相交接。
> 每次做完一段工作，在这里补一条记录：做了什么、遇到什么问题、下一步该谁接手。
> 格式：`## YYYY-MM-DD · 设备/执行方 · 阶段` 然后写清楚做了什么、留了什么坑、下一步是什么。

## 使用规则

1. 开始工作前，先读这份文件的最新记录，了解上一次做到哪、留了什么问题
2. 做完一段有意义的工作（不用每个小改动都记），补一条新记录
3. 遇到需要用户拍板的决策，写清楚"需要用户确认："，不要自己猜着往下做
4. 提交代码时，git commit message 里注明对应 WBS 的哪个阶段

---

## 记录

## 2026-07-11 · 华为服务器(Claude Code) · 阶段一：地基（数据库 + 后端 API 骨架）

**做了什么：**

- `api/main.py` 的 `init_db()` 里新建了 `users`/`books`/`chapters`/`highlights`/`reading_progress` 五张表，`qa_history` 加了 `user_id BIGINT DEFAULT 1` 字段，去掉了原来 90 天自动清理的 `DELETE` 语句
- 新增三个 `/app` 前缀骨架接口，鉴权直接复用现有的 `ExtAuth`（HMAC 会员卡式验证），没有另起一套：
  - `POST /app/books/import`：上传 EPUB，用 `ebooklib` 解析标题/作者/章节目录（优先读 EPUB 自带 toc，toc 缺失时回退到 spine 文档顺序兜底），写入 `books` + `chapters`
  - `GET /app/books`：书架，返回书本列表 + 各自阅读进度
  - `GET /app/books/{book_id}/context`：书本信息 + 章节目录 + 上次读到的位置
- `requirements.txt` 加了 `EbookLib`、`python-multipart`（后者是踩坑后补的——文件上传接口没装这个包直接启动会报错）
- 本地起了一个 PostgreSQL 18 + pgvector 测试库（跟 Railway 上的生产库完全隔离），实际跑通了服务、用一个自造的测试 EPUB 完整测试了三个新接口的全链路（导入→查书架→查详情，标题作者章节目录都解析对了），还额外验证了改造后的 `qa_history` 没有影响到 `extension/` 原有的 `/history` 接口（旧读写都正常）
- 全部工作在独立分支 `phase-1-foundation` 上，没有碰 `main`

**没有做的（按 WBS 阶段一范围排除在外）：**

- AI 对话逻辑、界面样式（阶段四、阶段二的事）
- `save_highlight`/`ask_about_passage`/`text_to_speech` 等接口（阶段三、四）
- 手机端 App 里实际调用这些新接口的代码（`mobile/` 目录还没动）

**留的坑 / 需要注意：**

- `import_book` 现在把 EPUB 原文件存在服务器本地 `epub_storage/` 目录（`.gitignore` 已排除）。这在 Railway 部署环境下是**临时可用但不持久**的方案——如果 Railway 的文件系统在重新部署时会被清空，之前导入的书会连带内容一起丢失（`books`/`chapters` 记录还在，但源文件没了）。阶段二真正筹备正式书库、部署到 Railway 测试时需要验证这一点，必要时换成对象存储（S3 等），现在不确定要不要现在处理，**需要用户确认**：要不要在阶段二之前先验证一下 Railway 文件系统持久性，还是先假设够用、遇到问题再换存储方案（呼应设计文档里"不为不存在的问题提前买单"的原则，我倾向于先不处理，但这个判断应该你来定）
- 本地测试用的 PostgreSQL 18，跟设计文档里提到的生产环境版本可能不完全一致（文档没写明生产是哪个大版本），实际部署前建议确认 Railway 上的 Postgres 版本，理论上不影响，但没有实测过

**下一步：** 阶段二——筹备 1-2 本公版经典 EPUB、用 `import_book` 实际导入、开始做书架界面。

---

## 2026-07-11 · 用户+这台电脑的 Claude · 回复阶段一遗留问题 + 阶段二章节切分问题

**关于 Railway 文件系统持久性（阶段一遗留问题）：**

已查证 [Railway 官方文档](https://docs.railway.com/volumes)——**Railway 的默认文件系统是临时的，每次重新部署都会被清空**，这不是"可能发生的边缘情况"，是**下次重新部署就一定会触发**的数据丢失。跟"不为不存在的问题提前买单"这条原则不适用于这里，因为这个问题不是假设性的。

**决定：现在就处理，不推迟。** 给这个服务在 Railway 后台挂一个持久卷（Volume），挂载到 `epub_storage/` 目录——这是 Railway 内置功能，不用改代码、不用换成 S3 这类对象存储。请华为那边确认一下当前 Railway 服务是否已经挂了 Volume，没有的话加上，然后验证一下重新部署后之前导入的书是否还在。

**关于 EPUB 章节切分信号（阶段二当前问题）：**

方向同意，但请先检查章节标题段落有没有 `<h1>`-`<h6>` 语义化标签或 `id` 属性——这个信号比 `margin-top` 这种 CSS 样式匹配更稳定，不会因为 Gutenberg 转换脚本换版本就失效。没有的话再退回 `margin-top` 方案。

另外：**这个切分逻辑不需要追求能应付任意未来 EPUB 的完美通用算法**——v1 书库是人工筛选的精品库，不是自动化管道，每本书上架前都会人工确认。切分完之后，请把实际渲染出来的章节结构截图/展示一下，人工确认切得对不对，而不是只看代码跑通、返回值没报错。

这次的教训（EPUB 并非都按章拆分，Gutenberg 尤其如此）已经补进 `docs/学习笔记/03-EPUB书库渲染与划线定位.md`。

**下一步：** 华为那边继续阶段二，处理完 Railway Volume 之后按上述方式调整章节切分逻辑。

---

## 2026-07-11（续）· 用户+这台电脑的 Claude · 章节切分方案定稿 + 一次战略考量（未采纳，记录留档）

**章节切分最终方案（级联式启发式规则，按可靠程度从高到低依次尝试，前面能确认就不用往下试）：**

1. EPUB 自带的官方目录（nav.xhtml / NCX）——最可靠，但 Gutenberg 这两本书目录本身是坏的，用不上
2. 语义化标题标签（`<h1>`-`<h6>`）或 `id` 属性——**优先尝试这个**，不会因转换脚本换版本失效
3. 命名锚点（如 `pgepubid` 这类 Gutenberg 常见的内部编号）——次选
4. CSS 样式匹配（如今天发现的 `margin-top`）——**最后的备用手段**，风险是会"悄悄切错却不报错"，不能作为唯一依据

不需要追求"能应付任意未来 EPUB 的完美算法"——v1 书库人工筛选，每本书上架前人工确认即可。以后如果书库规模扩大到人工看不过来，或者开放用户自行上传，可以参考 2026 年主流 PDF 解析工具（如 Reducto）的"两层叠加"模式：级联规则先跑一遍（快、便宜），再让 AI 检查纠正（替代人工质检），不用整个换成"AI 看图"式的重型方案——现在的问题复杂度（纯文字，无表格/多栏排版）用不上那种量级的方案。

**战略考量（讨论过，决定不采纳，留档避免以后重复讨论）：**

讨论过要不要把产品定位从"读书陪伴"扩大成"万能文档讲解"（用户上传任意 PDF/Word/图片，划选任意内容，AI 语音讲解+苏格拉底提问，跟点读笔一样）。市场潜力可能更大，且用户上传自己的文档能完全绕开 DRM 限制。**但决定暂不采纳，继续按现有"公版经典精读"方向做**，理由：

1. "万能文档讲解"赛道拥挤（Google NotebookLM 等大公司已在做），"精读经典+苏格拉底陪伴"反而是 DRM 限制意外造就的差异化窄众定位，扩得越广这个优势越稀释
2. 技术上确认了**不会造成浪费**：AI 大脑（DeepSeek 对话、语音架构、RAG、`ask_about_passage` 这类"独立能力"接口设计）完全格式无关，以后真要扩展到 PDF/图片，这层原样复用；会作废/需要重做的只是 EPUB 专属的部分——渲染方案（WebView+epub.js）、CFI 划线定位、`books`/`chapters` 这套偏"书"形状的数据库设计。这是"两条腿走路"那次决策（后端按独立能力设计，不绑定单一形态）现在开始回报的地方。

**结论：现有架构不需要为这个假设性的更大方向改动，继续推进阶段二。**

---

## 2026-07-11（续）· 华为服务器(Claude Code) · Railway 持久卷：已处理完成

**做了什么：**

- 查证 `api/railway.toml` 里的 `[[volumes]] mountPath = "/data"` 只是代码里的意图声明，**Railway 项目里从未真正创建挂载过**（`railway volume list` 显示项目里唯一的卷是 Postgres 自己的 `postgres-volume`，`BanduJiangJiang` 服务本身一个卷都没有）
- 用 Railway CLI（项目级token）实际创建了一个新卷 `bandujiangjiang-volume`，挂载到 `BanduJiangJiang` 服务的 `/data`
- 改了 `api/main.py` 里 `EPUB_STORAGE_DIR` 的默认值逻辑：`/data` 目录存在就用 `/data/epub_storage`（Railway环境），不存在就退回相对路径 `epub_storage`（本地开发环境），不用手动配置环境变量
- **实际做了端到端验证**（不是只看代码/配置就假设没问题）：把这次修复推到 main → Railway 自动重新部署 → 通过真实生产API上传一本测试书 → 手动触发再一次重新部署 → 确认数据库记录（书名/作者/章节）跨部署后还在。测试数据已清理，没有留在生产库里。

**留的坑（诚实说明，没有做到100%证明）：**

- 验证到数据库记录跨部署存活，这确认了"导入流程本身没出错"（因为代码逻辑是文件写入成功后才会写数据库记录），但**没能直接确认底层 EPUB 原始文件本身在 `/data` 卷里的存在性**——Railway CLI 的 `volume files` 和 `ssh` 命令都要求 SSH 密钥/账号级权限，我这边只有项目级token，配置SSH这条路走不通。基于"Volume 是 Railway 官方文档保证的持久化功能，不是实验特性"这个前提，加上已验证的间接证据，我认为这个问题已经解决，但如果之后真的出现书导入后文件读取失败的情况，第一个该怀疑的就是这里没有被100%坐实。

**重要发现（跟这次任务本身无关，但影响以后怎么做事）：**

- **Railway 对这个项目是"一推送到 main 就自动重新部署"**——也就是说阶段一那次合并到main的时候，其实已经自动上线到这个真实生产服务了（这个服务同时也是 `extension/` 在用的真实后端）。以后每次决定合并到 main，实际上就是在决定"现在要不要更新生产环境"，这两件事在这个项目里是一回事，不是"合并代码"和"部署上线"分开的两步。之前几次合并没出问题（改动本身也确实是纯新增，没破坏旧接口），但这个认知补上，以后过一遍脑子再合并。

**下一步：** 阶段二剩下的部分——EPUB 章节切分逻辑按你定的级联规则重做（语义标签 → 命名锚点 → CSS样式兜底），然后实际渲染验证给你看。

---

## 2026-07-11（续）· 华为服务器(Claude Code) · 内容筹备：两本书章节切分完成 + 实际导入验证

**做了什么：**

- 从 Project Gutenberg 下载了《道德经》《论语》两本公版书（零版权风险，官方核实过公版状态）
- 按级联规则实测（不是直接假设哪层能用，是真的逐层试）：
  1. 官方目录——两本书都是坏的（只登记了版权页），确认跳过
  2. `<h1>`-`<h6>` 语义标签——实测两本书里都没有可用的（唯一的h2标签是版权页标题），跳过
  3. 命名锚点（pgepubid等）——实测也没有，跳过
  4. 兜底方案，**中间踩了两次坑，都如实记录**：
     - 第一次试 `margin-top` CSS样式：《道德经》81章只切出7章，漏检严重，弃用
     - 第二次改成"短段落"：《道德经》完美命中81章+2个分卷标题，但同一规则用在《论语》上会把"20. 子不語怪力亂神。"这类恰好也短的"编号语录"误判成标题
     - **最终方案**：短段落 + 包含"第"字（真正标题都是"第一章"/"學而第一"这种格式，编号语录是"阿拉伯数字+句号"开头不含"第"）——两本书都精确切分，《道德经》81章、《论语》20篇，与传世版本篇章结构完全吻合
- 把切分结果重新打包成真正按章节分文件、带正常目录的干净EPUB（`content_source/*_clean.epub`），这样才能被现有 `import_book` 接口正常解析（不用改动 `_extract_chapter_titles` 的官方目录检测逻辑，因为干净EPUB的目录本身就是好的，第一层就能命中）
- 人工核对了完整章节标题列表（不只是看返回值没报错），还抽查了首章/末章正文内容，确认没有被截断或打乱
- 本地起测试环境，通过真实的 `/app/books/import` → `/app/books` → `/app/books/{id}/context` 完整链路导入验证，结果和预期一致
- 原始文件、清洗后文件、打包脚本都提交进了 `content_source/` 目录，写了 README 说明

**下一步：** 把这两本书实际导入到 Railway 生产数据库（这次是真实上架内容，不是要清理的测试数据），然后开始书架界面（读取 `get_library` 渲染书本列表）。
